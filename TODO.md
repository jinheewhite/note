# 판서노트 — 이어서 할 일

마지막 정리: 2026-08-21 (24.8 배포 완료)

---

## 지금 상태

- 배포된 판: **24.8** — https://jinheewhite.github.io/note/
- 안내서: https://jinheewhite.github.io/note/guide.html
- 남은 변경 없음 (모두 커밋·푸시 완료)

새 판이 안 보이면 → https://jinheewhite.github.io/note/fix.html

---

## 아직 안 만든 것 (다음에 이어서)

### 1. 도형 그리기

삼각형·사각형·원·직선을 끌어서 놓고, 색과 굵기를 고를 수 있게.

**설계 (조사 끝남)**

도형을 특별한 것으로 두지 않고 **보통 획으로 만듭니다**. 그러면 저장·
내보내기·지우개·되돌리기가 손댈 것 없이 그대로 통합니다.

- 새 도구 `S.tool='shape'`, 어떤 도형인지는 `S.shape`
  (`rect` · `tri` · `oval` · `line`)
- 도구모음에 `btnShape` 를 가위 옆에 둡니다. 고른 상태에서 한 번 더
  누르면 `openPop` 이 열리고, 거기에 도형 고르는 줄을 얹습니다
  (색·굵기 줄은 이미 있습니다).
- 누른 자리에서 끄는 자리까지로 점을 만들고 `S.cur.pts` 에 넣은 뒤
  `redraw()` — `redraw` 는 `S.cur` 도 그리므로 미리보기가 그대로 됩니다.
- 손을 떼면 `snap('pdf')` 뒤 `S.strokes.push`.

**주의 — 곡선 다듬기**

`paintStroke` 는 점이 셋 이상이면 `smoothPath` 로 이어, 사각형·삼각형의
모서리가 둥글게 뭉갭니다. 획에 `sharp:1` 을 두고 그 경우에는 곧은 선으로
잇도록 세 곳을 함께 고쳐야 합니다.

- `paintStroke` (PDF 화면)
- `bdPaint` (칠판 화면)
- `buildPdf` 안의 획 그리는 곳 두 군데 (PDF 쪽 · 칠판 쪽)

점 만들기 (화면 좌표로 만든 뒤 `toN` 으로 옮깁니다)

    line  두 점
    rect  네 귀퉁이 + 처음 점으로 닫기
    tri   위 가운데 · 오른쪽 아래 · 왼쪽 아래 + 닫기
    oval  48 점 정도의 타원

### 2. 칠판에서 오리기 (가위)

지금 `S.tool==='cut'` 은 PDF 쪽 손짓에서만 다룹니다. 칠판 손짓에도
같은 길을 내야 합니다.

- 칠판은 이미 그림(`BD.imgs`)·고르기(`BD.sel`)·손잡이(`drawHandles`)·
  크기 조절이 다 됩니다. 없는 것은 **오려서 만드는 길** 하나뿐입니다.
- 칠판에서 오린 조각은 PDF 로 보낼 수 있어야 합니다 (지금 반대 방향인
  `imgToBoard` 는 있습니다).

### 3. 오린 조각 크기 조절

PDF·칠판 모두 `drawHandles` 로 손잡이가 그려지고 오른쪽 아래로 크기를
바꿉니다. **되는지 실제로 눌러 보고**, 안 되면 그때 고칩니다.

---

## 내일 전자칠판에서 확인할 것

1. **자료가 안 죽고 열리는지** — 21.8 에서 그림판 예산을 504MB 에서
   84MB 로 줄였습니다. 아직 실물에서 확인하지 못했습니다.
   혹시 꺼져도 다시 열면 24.4 의 장치가 더 가볍게 엽니다.
2. 두 번째 열 때 곧바로 열리는지 (22.3)
3. 도구모음이 한 줄에 다 보이는지 — 자료 닫기·종료까지
4. 두 손가락으로 확대되는지 (24.7 에서 고쳤습니다)
5. `check.html` 화면 — 기기 메모리·화면 크기

---

## 손대는 방법 (사고 없이)

    G:\내 드라이브\앱만들고놀기\note\build\app.template.html   ← 여기만 고칩니다
    cd build && python build.py                                ← index.html 을 만듭니다
    index.html 을 판서노트.html · _USB용 · _오프라인 으로 복사
    APP_VER · sw.js 의 CACHE · version.json 세 곳의 판 번호를 함께 올립니다

- 파일을 쓸 때는 **임시 파일에 다 쓰고 바꿔치기**. 곧바로 열어서 쓰면
  중간에 잘못될 때 파일이 0바이트로 날아갑니다 (한 번 겪었습니다).
- 내보내기 전에 `<script>` 마다 문법 검사를 돌립니다.
