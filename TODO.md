# 판서노트 — 이어서 할 일

마지막 정리: 2026-08-28 (25.17 배포)

---

## 지금 상태

- 올라가 있는 판: **25.17** — https://jinheewhite.github.io/note/ (2026-08-28 배포)
  (25.16 은 푸시하지 못한 채로 있어서, 25.17 에 함께 실려 나갔습니다)
- 안내서: https://jinheewhite.github.io/note/guide.html

### 25.17 (2026-08-28) — 끝을 올려 두고, 도구를 옆에 세우고, 칠판지우개

전자칠판이 **낮게 걸려 있는 교실**에서 나온 세 가지를 한꺼번에 고쳤습니다.

#### 1. 쪽의 끝부분을 위로 더 올리고, 그 자리에 머뭅니다

쪽의 마지막 줄이 손 닿지 않는 아래쪽에 깔려, 눈높이까지 끌어올려 놓고
설명하고 싶은데 두 가지가 막혀 있었습니다 — **더 올라가지 않고**(종이
끝에서 멈춤), **올려도 머무르지 않았습니다**(손을 떼면 `settleView()` 가
도로 끌어내림).

`limits()` 를 규칙 하나로 다시 썼습니다 — **종이가 '보이는 자리'와 적어도
`keep` 만큼은 겹쳐 있어야 한다** (`keepRoom(v,paper)=min(v,paper,max(160,v*0.35))`).

- 그만큼만 남으면 **네 끝 어디로든** 밀 수 있습니다. 세로 800px 화면에서
  약 500px 더 올라갑니다 (전에는 150px, 그나마 되돌아왔습니다).
- '보이는 자리' 는 화면 전체가 아니라 **칠판을 뺀 자리**(`bdCover()`)입니다 —
  칠판에 가린 곳을 보려고 옆으로 미는 것은 그대로 됩니다.
- `clampView()` 에서 `over` 인자를 없앴습니다. **끄는 중이든 손을 뗀 뒤든
  같은 범위**를 쓰므로 되돌아오지 않습니다. 이것이 '머무르기' 입니다.
- 올려 둔 여유를 '쪽 넘기려는 뜻' 으로 오해하면 안 되므로, `limits()` 가
  **두 벌**을 돌려줍니다 — 실제 범위(`x0·x1·y0·y1`)와, 종이 끝이 보이는
  자리 끝에 딱 맞는 자리(`ex0·ex1·ey0·ey1`). 끝 판정과 쪽 넘김
  (`flipDirFrom`·`autoFlip`·`startPan`·`startPinch`·휠)은 뒤엣것을 씁니다.

#### 2. 양옆에 세로 도구막대 — 접었다 폈다 됩니다

화면 아래 도구모음까지 손이 잘 가지 않아, 자주 쓰는 것만 **화면 가운데
높이 양옆**에 세웠습니다. 도구모음 20개를 통째로 세우면 세로 1000px 이
넘어 화면에 안 들어갑니다.

- 세운 것: **지우개 · 펜 · 레이저 · ✋이동 · ↩되돌리기** (아래 막대와 같은 차례)
- **아래 막대는 그대로 둡니다** — 둘이 동시에 살아 있고, 편한 쪽을 쓰면 됩니다
- 넓은 전자칠판에서 어느 쪽에 서 있든 걸어가지 않게 **왼쪽·오른쪽 둘 다**
  (지우개를 도구모음 양 끝에 둔 25.13·25.15 의 뜻과 같습니다)
- **기본은 양쪽 다 펴진 채**. 맨 위 손잡이로 **왼쪽·오른쪽을 따로** 접습니다.
  접어도 손잡이는 남깁니다 — 아주 없애면 다시 불러낼 길이 안 보입니다.
  접어 둔 것은 기억합니다 (`pn.sideHid.L` · `pn.sideHid.R`).
- 아래 막대 단추를 **복제해서** 만듭니다(`buildSideTools()`). 그림(SVG)을
  또 베껴 두면 한쪽만 고쳤을 때 서로 달라집니다.
- 도구 단추마다 **`data-tool`** 을 달아, 같은 도구는 어디에 있든 함께
  켜집니다. 지우개만 따로 챙기던 코드가 없어졌고, `showColor` 도
  `$('btnPen')` 하나가 아니라 `[data-tool="pen"]` 전부를 칠합니다.
- 되돌리기는 도구가 아니라 '한 일 무르기' 라 `data-act="undo"` 로 답니다.
  `updUndo()` 가 그 표를 단 것 **전부**를 함께 껐다 켭니다.
- 왼쪽 막대는 쪽번호 줄(`aside`, 78px)을 비켜 세웁니다(`left:88px`).
  칠판을 왼쪽으로 옮기면 좌우를 맞바꾸고, 가로 자료(`rail-h`)·칠판
  단독(`board-full`)이면 둘 다 바깥 끝까지 붙입니다.

#### 3. 지우개를 칠판지우개로, 크기는 굵기에 맞춰

- 그림이 **연필 지우개(고무)** 였습니다. 전자칠판에서 쓰는 것이니
  **칠판지우개**로 다시 그렸습니다 — 입체 상자에 **위를 가로지르는
  손잡이 띠**, 아래 모서리에 흰 펠트 (쓰시는 분이 직접 그려 주신 모양입니다).
- 커서가 **굵기와 상관없이 늘 같은 크기**여서 얼마나 지워질지 알 수
  없었습니다. 이제 실제로 지워지는 넓이에 맞춥니다 —
  `eraserPx() = max(16, 굵기*2.2) * 2` → 굵기 6·10·18·30 이 32·44·79·132px.
- 마우스 커서만은 **120px 에서 자릅니다.** 크롬은 128px 을 넘는 커서 그림을
  통째로 버려서, 안 자르면 굵은 지우개에서 커서가 **아예 사라집니다.**
  손·펜으로 쓸 때 뜨는 그림(`ghost`)은 DOM 이라 제한이 없어 제 크기 그대로입니다.
- `showSize()` 끝에 `applyCursor()` 를 넣어, **굵기를 고르는 순간** 커서가
  따라 바뀝니다 (전에는 도구를 다시 골라야 했습니다).

#### 아직 실물에서 못 본 것

이번 세 가지 모두 **실제 전자칠판에서는 아직 못 봤습니다.** 특히 —

- 오른쪽 세로 막대가 칠판 쓰는 자리를 가리지 않는지 (가리면 접으면 됩니다)
- 세로 막대 단추 크기(58px)와 접기 손잡이(22px)가 손에 맞는지
- 가장 굵은 지우개(132px)가 지나치지 않은지
- 종이를 올릴 수 있는 여유(화면의 65%)가 알맞은지 — 모자라거나 지나치면
  `keepRoom()` 의 `0.35` 하나만 고치면 됩니다

### 25.16 (2026-08-26) — 마우스로 끌 때도 쪽이 안 넘어갑니다

25.13 에서 손가락 하나(touch)는 끝에서 멈추게 고쳤는데, **마우스·펜으로
✋ 손 도구를 끌 때는 일부러 옛 방식(끝을 넘기면 곧바로 다음 쪽)을
남겨 뒀었습니다** — 데스크톱 시험용으로 남긴 것이었는데, 실제로 크롬
에서 문서 끝을 보려고 마우스로 위로 끌다가 똑같이 자꾸 다음 쪽으로
넘어간다는 걸 확인했습니다(스크린샷으로 재현). 그래서 이번에는 마우스
예외를 없애고, **어떤 포인터로 끌든 끝에서는 그 자리에 멈추도록**
통일했습니다.

- `autoFlip`(끄는 도중) — 넘기던 부분을 통째로 지우고, 끝에 닿았다는
  안내(`edgeTag`)만 남겼습니다.
- `settleView`(손을 뗄 때) — 이제 인자 없이 늘 `clampView();applyView()`
  만 합니다. 넘기는 갈래를 지웠습니다.
- `swipeFlip`(훅 긋고 놓기) — 마우스만 남겨 뒀던 함수라 완전히 지웠고,
  `release()` 의 호출도 없앴습니다.
- 이제 쪽을 넘기는 길은 **두 손가락으로 끝에서 힘차고 빠르게 밀기**
  (`movePinch`, 터치 전용) 하나뿐입니다 — 그 밖엔 ▶/◀ 단추나 방향키
  같은 분명한 조작만 넘깁니다.
- 지우다가 쓰던 사라진 함수(`flipDir`)도 이제 안 쓰여 지웠습니다
  (`flipDirFrom` 만 남았고, `movePinch` 가 그것만 씁니다).
- **오른쪽 끝 지우개(`btnEraser2`)를 눌렀을 때 크기 고르기 창이 화면
  한가운데쯤에서 열리던 것을 고쳤습니다.** `placePop` 은 원래 "오른쪽에
  자리가 남으면 오른쪽으로" 만 봤는데, 오른쪽 끝 단추라도 창 너비보다
  살짝 더 남는 자리가 있으면 그 얼마 안 되는 자리로 열려 화면 가운데쯤
  까지 밀려났습니다 — 반대쪽 끝까지 걸어가지 않으려고 지우개를 양쪽에
  둔 뜻이 무색해졌습니다. 이제 **단추가 화면 오른쪽 절반에 있으면
  무조건 왼쪽으로, 왼쪽 절반이면 오른쪽으로** 엽니다(자리가 모자라면
  화면 안쪽으로 마저 당겨 넣습니다). 스크린샷으로 재현해 확인한 것입니다.

### 25.15 (2026-08-26) — 지우개를 도구모음 진짜 양쪽 끝으로

25.13 에서 오른쪽 끝에 지우개(`btnEraser2`)를 더했었는데, 왼쪽 지우개
(`btnEraser`)는 `#tools` 안 맨 마지막(펜·형광펜·레이저·도형·오리기 다음)
이라 실제로는 '왼쪽 끝'이 아니었습니다 — 고쳤습니다.

- **왼쪽 지우개를 `#tools` 의 맨 앞으로 옮겼습니다.** 이제 도구모음
  왼쪽 끝(맨 처음 단추)이 지우개이고, 오른쪽 끝(맨 마지막 단추)도
  지우개라, 양쪽 끝에서 걸어가지 않고 지울 수 있습니다.
- **두 지우개 그림을 마주 보게 뒤집었습니다.** `#btnEraser2 svg{transform:
  scaleX(-1)}` — 왼쪽 지우개는 그림이 오른쪽(가운데)으로 펼쳐지고,
  오른쪽 지우개는 왼쪽(가운데)으로 펼쳐집니다.

### 25.14 (2026-08-26) — 자료 쪽에서도 손바닥이 지우개

- **PDF·자료 쪽에서도 손바닥이 지우개가 됩니다.** 전자칠판처럼 "펜은 글씨를
  쓰고, 손바닥은 지운다" 는 말이 나와서 바꿨습니다. 칠판(`BD`)에는 이미
  있던 것을 자료 쪽(`inkCv`)에도 맞췄습니다. `isPalm(e)` 로 손바닥을
  가리면(다른 손가락이 이미 닿아 있거나, 접촉이 22px 이상으로 넓으면),
  ✋ 손 도구를 일부러 골라 둔 게 아닌 한 지금 무슨 도구를 쓰고 있든
  `S.palmErase` 를 켜고 `erase()` 를 부릅니다(`startStroke`·`moveStroke`
  맨 앞). ✋ 손 도구를 골라 둔 상태의 손바닥은 그대로 화면 이동입니다 —
  일부러 이동을 고른 것이니까요. 손가락 하나(좁은 접촉)로 미는 것도
  그대로 화면 이동입니다 — 넓은 '진짜 손바닥'만 지우개로 바뀝니다.
- **지우는 동안 커서가 지우개 그림으로 바뀝니다.** `ghostShow`·`ghostMove`
  가 `S.palmErase` 를 보도록 고쳐, 지금 고른 도구(펜 등)와 상관없이
  손바닥으로 지우는 동안은 지우개 그림이 접촉점을 따라다닙니다.
- **아직 실물 전자칠판에서 못 봤습니다** — 손바닥 폭 문턱(22px)이 그
  기기의 터치 인식과 맞는지, 펜을 쥔 손이 자꾸 오인식되지 않는지 확인
  이 필요합니다(펜이 눌려 있는 동안은 다른 터치를 통째로 무시하는 옛
  규칙은 그대로 두어, 쥔 손 자체는 여전히 걸러집니다).

### 25.13 (2026-08-26) — 지우개 양쪽 끝 · 쪽넘김을 두 손가락으로 · 도형 다듬기에서 선 뺌

- **지우개 단추를 도구모음 양쪽 끝에 둡니다.** 왼쪽 지우개(`btnEraser`)는
  그대로, 오른쪽 끝(`rightGrp`)에 `btnEraser2` 를 새로 둡니다 — 넓은
  전자칠판에서 반대쪽에 선 사람도 걸어가지 않고 지울 수 있게 하려는
  것입니다. 둘 다 같은 도구를 고르므로, 어느 쪽을 눌렀든 `setTool` 이
  둘 다 켜진 것으로 보여 줍니다.
- **손가락 하나로 쪽 끝을 보다가 실수로 다음 쪽이 넘어가던 것을 껐습니다.**
  `autoFlip`(끄는 도중) · `settleView`(손을 뗄 때) · `swipeFlip`(훅 긋기) ·
  옛 `swTry`(아무 데서나 긋기, 이제 지웠습니다) 넷 다 손가락 하나(touch)
  일 때는 넘기지 않고 끝에서 멈춥니다. 마우스·펜으로 손 도구를 끄는
  것은 그대로 넘어갑니다(데스크톱에서 시험할 때 쓰던 방식이라 남겨 둠).
- **두 손가락으로 끝에서 힘차고 빠르게 밀 때만 다음/이전 쪽으로 넘어갑니다.**
  `movePinch` 의 pan 모드에 여유(`clampView(true)`)를 주고, 밀린 거리
  (`flipDist()+130`, 손가락 하나 때보다 더 멀리 밀어야 함)와 속도
  (`P.spd>=0.8px/ms`, 지수평균) 를 함께 봐서 그때만 `flip()` 을 부릅니다.
  안내 문구(`edgeTag`)도 손가락일 때는 "두 손가락으로 힘차게 밀면" 으로
  바뀝니다. **아직 실물 전자칠판에서 문턱값(130px·0.8px/ms)이 알맞은지
  못 봤습니다** — 너무 세게 밀어야 넘어가면 문턱을 낮춰야 합니다.
- **펜으로 슥 그려 다듬는 도형에서 '곧은 선'을 뺐습니다.** `tidyShape` 의
  1번 갈래(두 끝을 이은 선과 벌어진 정도를 보던 곳)를 지웠습니다 — 이제
  네모·세모·동그라미 셋만 다듬어지고, 곧게 그은 손글씨 획은 그대로
  남습니다. 도형 단추를 눌러 **일부러** 직선을 고르는 것은 그대로 됩니다
  (`shapePts` 의 `kind==='line'` 갈래는 안 건드렸습니다) — 이번에 뺀 것은
  펜으로 슥 그었을 때 저절로 직선으로 잡히던 자동 인식뿐입니다.
- **새 판 안내줄·"지금 업데이트" 단추를 없애고, 안내 없이 조용히
  새로고침하도록 바꿨습니다.** 전에는 `hardUpdate()` 가 서비스워커를
  등록 해제하고 캐시를 통째로 지운 뒤 3.5MB 를 처음부터 다시 받아
  아주 느렸습니다(전자칠판처럼 느린 기기·네트워크에서 특히). 이제
  `autoCheckUpdate` 는 새 판이 있으면 그냥 `location.reload()` 만
  합니다 — 서비스워커가 이번 열림에서 뒤에서 이미 받아 둔 파일이
  있으면 그 새로고침으로 바로 새 판이 되고, 아직 못 받았으면 그냥
  지금 판이 다시 열릴 뿐이니 세션마다(`sessionStorage.pn.updTries`)
  최대 4번만 조용히 다시 시도합니다. `updBar`·`updMsg`·`updNow`·
  `updLater` 는 이제 아무 데서도 안 써 완전히 지웠습니다. 설정 메뉴의
  "업데이트 확인"(`checkUpdate`→`hardUpdate`) 단추는 그대로 뒀습니다 —
  사람이 일부러 눌러 확실하게 다시 받고 싶을 때를 위한 것이라 남겨도
  됩니다.

### 25.11 에서 바꾼 것 (2026-08-21)

- **하트·별·화살표를 뺐습니다.** 하트와 별은 손으로 그린 것을 알아채는
  자리에서 자꾸 엉뚱하게 잡혀 이상해졌습니다. 이제 도형은 넷뿐입니다 —
  **네모·세모·동그라미·직선**. 지운 것: `heartRaw`·`heartFit`·`starFit`,
  `tidyShape` 의 하트 갈래와 별 갈래 둘, `shapePts` 의 화살표 갈래,
  아이콘·이름·고르는 줄. 예전에 별을 골라 두었더라도 네모로 열립니다.
- **그린 도형을 바로 옮기고 늘립니다.** 방금 그린 도형에 노란 점선 테두리와
  손잡이 셋이 남습니다 — 왼쪽 위 ✥ 옮기기, 오른쪽 아래 ↘ 크기,
  오른쪽 위 ✕ 그 도형만 지우기. 도형 도구를 골라 둔 상태라면 테두리 안
  아무 데나 끌어도 옮겨집니다. 다른 곳을 누르면 그대로 놓입니다.
  PDF·칠판 양쪽 다 됩니다 (`shRect`·`shHitBox`·`shApply`·`drawShapeBox`,
  `S.shSel`/`S.shDrag` · `BD.shSel`/`BD.shDrag`).
  도형도 보통 획이라, 점을 화면 좌표로 펴서 옮긴 뒤 도로 넣습니다.
  곧은 선처럼 한쪽 폭이 0 에 가까우면 그쪽은 늘리지 않습니다.
  **펜으로 슥 그려 다듬어진 도형도 똑같이 잡힙니다** (`tidyCur` 가 참을
  돌려주면 그 획을 고른 것으로 둡니다). 처음엔 도형 도구로 그린 것만
  잡히게 해 두었는데, 정작 많이 쓰는 길이 펜으로 슥 그리는 쪽이었습니다.
  이때 테두리 안쪽은 잡지 않습니다 — 안 그러면 그 안에 글씨를 못 씁니다.
  그래서 ✥ 손잡이를 따로 답니다.
- **칠판에서 오려 PDF로 보내면 보이는 자리에 놓입니다.** 전에는 쪽의
  '맨 위 가운데' 로 보내서, 폭 맞춤으로 아래쪽을 보고 있으면 조각이
  화면 밖으로 사라졌습니다 (`imgToPdf` → 새 `pdfViewCenter`).
  칠판이 가린 자리는 빼고, 칠판만 보던 중이었으면 PDF가 보이게 되돌립니다.
  크기도 화면에 보이던 그대로 맞춥니다.

- **가위를 대각선으로 죽 끌면 그 네모만큼 오립니다.** 둘레를 한 바퀴
  두르는 예전 방식도 그대로 됩니다 — 새 `cutRectIfDiagonal` 이 '지나온 길'과
  '두 끝을 이은 선'을 견주어 저절로 갈라 줍니다 (길이가 1.35배 안이면 곧게
  끈 것). 끄는 도중 미리보기도 네모로 보입니다 (`cutDraw`·`bdCutDraw`).
  둘러서 오릴 때 방향 잡기가 어렵다는 말을 듣고 넣었습니다.

### 25.12 (2026-08-21) — '새 버전' 알림줄이 안 뜨던 것

`autoCheckUpdate` 안에서 `skipV`·`skipAt` 을 `try{}` 블록 안에 `let` 으로
두고, 그 아래 다른 블록에서 썼습니다. 블록을 벗어나면 없는 이름이라
`ReferenceError` 가 나고, 그것을 바깥 `catch(e){}` 가 조용히 삼켰습니다.
그래서 **새 판이 나와도 알림줄이 한 번도 뜨지 않았습니다** (25.10 에도
같았습니다). 두 값을 함수 자리로 옮겼습니다.

알림줄이 없어도 서비스워커가 뒤에서 새 파일을 받아 두므로 **두 번째
열 때** 새 판으로 열리기는 했습니다. 다만 "지금 업데이트" 를 눌러
그 자리에서 바꾸는 길이 막혀 있었습니다.

### 크롬으로 눌러 보고 확인한 것 (2026-08-21)

`build` 폴더를 `python -m http.server` 로 띄우고 크롬 확장으로 직접 눌렀습니다.

1. 도형 고르는 줄에 네모·세모·동그라미·직선 넷만 나옵니다 ✓
2. 도형 도구로 동그라미 → 테두리와 ✥·✕·↘ 가 붙습니다 ✓
   ✥ 로 옮기기 ✓ (끈 만큼 정확히) · ↘ 로 늘리기 ✓ (왼쪽 위 고정)
3. **펜으로 슥 그린 동그라미**도 다듬어지면서 똑같이 잡힙니다 ✓
   펜 도구인 채로 ✥ 를 끌어 옮기는 것까지 됩니다 ✓
4. 가위 대각선 → 끈 네모 그대로 오려집니다 ✓
5. 오린 조각 칠판으로 → 다시 PDF 로 → **보이는 자리에 놓입니다** ✓
   (일부러 확대·이동해 쪽 위쪽을 화면 밖으로 보낸 상태에서 확인)

주의 — 다시 만들 때마다 판 번호가 그대로면 서비스워커가 예전 것을 물고
있습니다. 시험할 때는 `caches.delete` 로 지우고 다시 불러야 합니다.

### 아직 눌러 보지 못한 것 (다음에 실물에서)

1. **칠판 쪽** 도형 손잡이 (크롬에서는 PDF 쪽만 눌러 봤습니다)
2. 칠판만 보던 중에 조각을 PDF 로 보내기 (`splitMode===2` 되돌리기)
3. 전자칠판에서 손가락·펜으로 손잡이가 잘 잡히는지 (동그라미 반지름 22)

새 판이 안 보이면 → https://jinheewhite.github.io/note/fix.html

---

## 다음에 이어서 할 만한 것

- **도형 돌리기** — 지금은 옮기기·크기만 됩니다. 점을 네모 가운데를 축으로
  돌리면 되는데, PDF 쪽은 좌표계가 가로세로 따로라(`toN` 은 x 를 쪽 폭으로,
  y 를 쪽 높이로 나눕니다) 반드시 화면 좌표로 펴서 돌린 뒤 도로 넣어야 합니다.
- **한 번 놓은 도형 다시 고르기** — 지금은 '방금 그린 것' 만 고른 상태로
  남습니다. 예전 것을 다시 고르려면 획을 찍어 찾는 길이 있어야 하는데,
  큰 동그라미가 화면을 덮으면 그 안에 새 도형을 못 그리게 되는 것이
  걸려서 일부러 넣지 않았습니다.

---

## 전자칠판에서 확인할 것 (예전부터 남은 것)

1. **자료가 안 죽고 열리는지** — 21.8 에서 그림판 예산을 504MB 에서
   84MB 로 줄였습니다. 아직 실물에서 확인하지 못했습니다.
2. 두 번째 열 때 곧바로 열리는지 (22.3)
3. 도구모음이 한 줄에 다 보이는지 — 자료 닫기·종료까지
4. 두 손가락으로 확대되는지 (24.7 에서 고쳤습니다)
5. `check.html` 화면 — 기기 메모리·화면 크기

---

## 손대는 방법 (사고 없이)

    G:\내 드라이브\앱만들고놀기\note\build\app.template.html   ← 여기만 고칩니다
    cd build && python build.py                                ← index.html 을 만듭니다
    index.html 을 판서노트.html · _USB용 · _오프라인 ·
      USB\판서노트.html · USB\panseo-note.html 로 복사 (다섯 벌 다 같은 파일)
    APP_VER · sw.js 의 CACHE · version.json 세 곳의 판 번호를 함께 올립니다

- 파일을 쓸 때는 **임시 파일에 다 쓰고 바꿔치기**. 곧바로 열어서 쓰면
  중간에 잘못될 때 파일이 0바이트로 날아갑니다 (한 번 겪었습니다).
- 내보내기 전에 `<script>` 마다 문법 검사를 돌립니다 (`node --check`).
  안에 든 `{"__CMAPS__":1}` · `{"__FONTS__":1}` 두 덩이는 자료라서
  문법 검사에서 걸리는 것이 정상입니다.
